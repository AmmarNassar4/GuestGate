<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <!-- Base URL of the server -->
    <DefaultBaseUrl>https://guestgate-ramada.ibaapps.work</DefaultBaseUrl>
    <!-- API base path (can be empty or /api or /backend/api ...) -->
    <ApiBasePath>/api</ApiBasePath>
    <!-- Tablets / Kids list -->
    <Kids>K1,K2,K3</Kids>
    <!-- Preferred tablet (will be updated from the app) -->
    <PreferredKid>K3</PreferredKid>
    <!-- Preferred template name or template ID -->
    <PreferredTemplateName></PreferredTemplateName>
  </appSettings>
</configuration>
